# Socket connection to crawd backend
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# TTS is entirely optional. Omit all TTS vars for text-only mode.

# Bot voice (used for bot speech)
# TTS_BOT_PROVIDER=openai
# TTS_BOT_VOICE=onyx

# Chat voice (used for reading chat messages in reply-turns)
# TTS_CHAT_PROVIDER=tiktok
# TTS_CHAT_VOICE=en_us_002

# Provider API keys (only needed for enabled providers)
# OPENAI_API_KEY=sk-...
# ELEVENLABS_API_KEY=...
# TIKTOK_SESSION_ID=...
